# TODO

## Comments

- [x] Draft persistence — незбережений текст живе в `dt-draft:<route>`, флаш на `pagehide`,
      відновлення при ініті. Картинка відпадає перша, якщо не влазить у квоту.
- [x] Панель коментарів завжди відкривається розгорнутою — згортання більше не памʼятається;
      згорнута панель розгортається кліком по всій шапці.

Обидва в `src/client/run.ts`, ще не закомічені, тег не піднятий.
